apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: k5wang-volume  #use your preferred name
spec:
  storageClassName: rook-cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
